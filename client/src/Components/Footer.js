import { useSelector } from "react-redux";
const Footer = () => {
  //const cuser = useSelector((state) => state.users.user);
  return (
    <footer className="footer">
      <div>Mithaq - ©2023.PostIT. All Rights Reserved.</div>
    </footer>
  );
};

export default Footer;
